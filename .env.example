# Character assets (served from public/)
VITE_CHARACTER_GIF_URL=/suda.gif
# Still frame shown when SUDA is idle (extract first GIF frame to public/suda-idle.png)
VITE_CHARACTER_IDLE_IMAGE_URL=/suda-idle.png

# Linear integration — read by the Tauri backend (do NOT prefix with VITE_)
# Restart `npm run tauri dev` after changing these values.
LINEAR_API_KEY=your_linear_key

# Legacy browser-only keys (not used by the desktop app)
# VITE_LINEAR_API_KEY=
# VITE_LINEAR_TEAM_ID=

# Google Calendar integration — disabled for now
# VITE_GOOGLE_CALENDAR_API_KEY=
# VITE_GOOGLE_CALENDAR_ID=

# ElevenLabs voice for SUDA TTS. Browser speechSynthesis is fallback only.
# Read by the Tauri backend — do NOT prefix with VITE_.
# Restart `npm run tauri dev` after changing these values.
# Voice debug logs: Tauri terminal (Rust) + webview devtools console ([SUDA] prefix).
ELEVENLABS_API_KEY=your_elevenlabs_api_key_here
ELEVENLABS_VOICE_ID=your_elevenlabs_voice_id_here
ELEVENLABS_MODEL_ID=eleven_multilingual_v2
ELEVENLABS_STABILITY=0.5
ELEVENLABS_SIMILARITY_BOOST=0.75
ELEVENLABS_STYLE=0
ELEVENLABS_USE_SPEAKER_BOOST=true
